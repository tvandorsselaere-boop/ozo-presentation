# OZO × Facile-IA — Présentation

Présentation client (deck statique HTML/CSS) pour OZO Electric.

- `index.html` — la présentation
- `styles-client.css` — styles

Hébergé sur Vercel. Ouvrir `index.html` localement ou via l'URL de déploiement.
